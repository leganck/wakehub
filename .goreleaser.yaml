# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: wol-server
    main: ./cmd/server
    binary: wol-server
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.buildTime={{.Date}}

  - id: wol-client
    main: ./cmd/client
    binary: wol-client
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.buildTime={{.Date}}

archives:
  - id: wol-server
    ids:
      - wol-server
    formats: [tar.gz]
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE*
      - README.md
      - .env.example

  - id: wol-client
    ids:
      - wol-client
    formats: [tar.gz]
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE*
      - README.md

dockers:
  - id: wol-server-amd64
    ids:
      - wol-server
    goos: linux
    goarch: amd64
    image_templates:
      - "ghcr.io/leganck/wol:latest-amd64"
      - "ghcr.io/leganck/wol:{{ .Tag }}-amd64"
      - "ghcr.io/leganck/wol:v{{ .Major }}.{{ .Minor }}-amd64"
    use: buildx
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.title=wol"
      - "--label=org.opencontainers.image.description=WOL server with Bemfa MQTT"
      - "--label=org.opencontainers.image.source=https://github.com/leganck/wol"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"

  - id: wol-server-arm64
    ids:
      - wol-server
    goos: linux
    goarch: arm64
    image_templates:
      - "ghcr.io/leganck/wol:latest-arm64"
      - "ghcr.io/leganck/wol:{{ .Tag }}-arm64"
      - "ghcr.io/leganck/wol:v{{ .Major }}.{{ .Minor }}-arm64"
    use: buildx
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.title=wol"
      - "--label=org.opencontainers.image.description=WOL server with Bemfa MQTT"
      - "--label=org.opencontainers.image.source=https://github.com/leganck/wol"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"

docker_manifests:
  - name_template: "ghcr.io/leganck/wol:latest"
    image_templates:
      - "ghcr.io/leganck/wol:latest-amd64"
      - "ghcr.io/leganck/wol:latest-arm64"

  - name_template: "ghcr.io/leganck/wol:{{ .Tag }}"
    image_templates:
      - "ghcr.io/leganck/wol:{{ .Tag }}-amd64"
      - "ghcr.io/leganck/wol:{{ .Tag }}-arm64"

  - name_template: "ghcr.io/leganck/wol:v{{ .Major }}.{{ .Minor }}"
    image_templates:
      - "ghcr.io/leganck/wol:v{{ .Major }}.{{ .Minor }}-amd64"
      - "ghcr.io/leganck/wol:v{{ .Major }}.{{ .Minor }}-arm64"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
